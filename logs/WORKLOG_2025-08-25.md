# Work Log — 2025-08-25

## Provider Auth + Dashboard
- Added provider login at `app/provider/login/page.tsx` with centered Tailwind UI and shadcn inputs/buttons.
- Implemented provider dashboard `app/provider/page.tsx` with sidebar (Patients/Appointments/Profile), Patients table, and "New patient" modal.
- Styled Patients table, added date formatting (mm/dd/yy).

## Provider APIs
- List intakes: `app/api/provider/intakes/route.ts` (returns name, email, status, timestamps).
- Create patient: `app/api/provider/create-patient/route.ts` (creates `clients/*` and initial `intakes/*`).
- Send intake link: `app/api/provider/send-intake/route.ts` (issues token, writes to `patient_links/{token}` and `intakes/{id}.patient_link`).

## Patient Link Flow
- Patient resolver: `app/api/patient/resolve/route.ts` (fast path via `patient_links/{token}`; fallback scan).
- Patient start page: `app/patient/start/page.tsx` (birthdate confirmation with Month/Day/Year dropdowns; redirects with `practiceId,intakeId,asPatient=1`).
- Intake bootstrap: `app/intake/Flow.tsx` now loads payload directly via `{practiceId,intakeId}` when `asPatient=1` and starts fresh locally; does not sign the provider out.
- Intake header hides login banner when `asPatient=1`.
- Birthdate display fixed (timezone-safe reformat to mm/dd/yyyy).

## Intake UX
- Sleep snapshot summary enhanced with approximate nightly duration.
- CEC full responses: sticky subheaders; now full-height (no inner scroll); grouped by domain; edit/save with dropdowns; persisted back to Firestore.
- "Welcome back" CTA only shows when `payload.intakeId` AND `payload.changed` exist.

## Gap Report
- Spec added `intake/specs/gap_spec.md` (provider-led patient-centric questions + separate "Why this matters").
- Generate: `app/api/provider/gap-report/route.ts` (uses OpenAI; caches `{markdown,json}` under `intakes/{id}.gap_report`; ensures at least one `rating_0_10` question).
- Save answers: `app/api/provider/gap-report/save/route.ts` (stores `gap_report.answers`).
- UI:
  - Gap button/modal `app/provider/patients/[intakeId]/GapReport.tsx`: spinner while generating; auto-closes and switches to Gap tab; hides raw MD/JSON.
  - Gap tab form `app/provider/patients/[intakeId]/GapReportForm.tsx`: section headers; question cards with subtle border/shadow; 0–10 slider using shared `components/ui/slider.tsx` with tick labels; text inputs for free-form.

## Misc
- Provider detail shows ISO birthdate as mm/dd/yyyy without timezone shifts.
- Fixed duplicate imports and minor lints.
- Seed helpers used via terminal to clear/restore test docs and tokens.

## Files Touched (high-level)
- Pages: `app/provider/*`, `app/patient/start/page.tsx`, `app/intake/page.tsx`, `app/intake/Flow.tsx`
- APIs: `app/api/provider/{intakes,create-patient,send-intake,gap-report,gap-report/save}/route.ts`, `app/api/patient/resolve/route.ts`, `app/api/intake-load/route.ts`
- UI: `components/ui/{button,input,slider}.tsx`, `components/intake/StepHeader.tsx`
- Gap UI: `app/provider/patients/[intakeId]/{GapReport.tsx,GapReportForm.tsx}`
- Spec: `intake/specs/gap_spec.md`

## Testing Notes
- Patient link: create via dashboard → copy URL → open `/patient/start?token=...` → confirm birthdate (MM/DD/YYYY) → redirected into intake with correct payload and no login banner.
- Gap: generate → modal spinner → auto-switch to Gap tab → sliders for `rating_0_10`, text inputs otherwise → Save persists.
